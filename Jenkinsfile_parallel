pipeline {
    agent any
    stages {
        stage(“Compile and Build Binary”) {
            parallel {
                stage(“dockerbuild”) {
				steps {
					sh """
					echo "build"
					"""
					}
				}
            
                stage(“dockerbuild”) {
     			steps {
					sh """
					echo "build"
					"""
					}
                }
			}
        }
   
	   stage('docker build/push') {            
			steps {
			   sh """
				echo "push image"
				"""
			}
	   }     
	   }
}
